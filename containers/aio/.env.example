PORT=3000
VKARA_AIO=1

# Used by API for YouTube embed origin checks. Override when deploying behind a public domain.
PUBLIC_APP_URL=http://localhost:3000

# Sentry — set both DSNs in the container env (api + web are separate projects)
# Environments: development | production only (see packages/env/src/sentry.ts)
# SENTRY_DSN=                 # vkara-api (passed to API process)
# NEXT_PUBLIC_SENTRY_DSN=     # vkara-web (baked/available to Next)
# SENTRY_ENVIRONMENT=production
# NEXT_PUBLIC_SENTRY_ENVIRONMENT=production
# SENTRY_TRACES_SAMPLE_RATE=0.1
# SENTRY_AUTH_TOKEN=          # optional: source map upload at image build

# Optional: speech transcription via external Whisper service
# WHISPER_URL=
# HF_TOKEN=

# Optional: LLM karaoke-song judge for the "Pedir canción" flow.
# Without a key the pipeline runs in heuristic mode (ranking sin LLM).
# KARAOKE_AGENT_PROVIDER=heuristic   # heuristic | anthropic | openai
# KARAOKE_AGENT_API_KEY=
# KARAOKE_AGENT_MODEL=claude-3-5-haiku-latest
